"""Optional add-ons, each switched on by one call on `FastFort`.

`audit` is the audit log (`fort.enable_audit_log`). Soft delete and rate
limiting, once promised here, landed in `admin/` and `auth/` instead.
"""

from __future__ import annotations
